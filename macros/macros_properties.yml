version: 2

macros:
  - name: get_connection_type
    description: >
      This macro receives a connection_type and returns the corresponding description.
    arguments:
      - name: connection_type
        type: string
        description: > 
          connection_type value.
          Must be one of the accepted values, otherwise the macro will return null